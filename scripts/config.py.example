# UIUC CS525 Cluster Configuration Template
# Copy this file to 'config.py' and fill in your details.
# DO NOT commit 'config.py' to Git.

USER = "your_netid"
BASE_URL = "sp26-cs525-06"
DOMAIN = ".cs.illinois.edu"

# Optional: Path to your private SSH key if not using the default one
# SSH_KEY_PATH = "C:/Users/yourname/.ssh/id_rsa"

# Optional: Password for sudo commands (needed for cluster_power.py)
# PASSWORD = "your_password_here"
